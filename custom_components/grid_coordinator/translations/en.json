{
  "config": {
    "step": {
      "user": {
        "title": "Grid Coordinator Setup",
        "description": "Configure hard grid limits and control parameters. Entity IDs are set in const.py for the MVP.",
        "data": {
          "import_limit": "Import limit (W)",
          "export_limit": "Export limit (W)",
          "ramp_step": "Ramp step per 10 s tick (W)",
          "plan_stale_minutes": "EMHASS plan stale after (minutes)",
          "mpc_sign_inverted": "EMHASS mpc_grid_power positive = export to grid (invert sign)"
        },
        "data_description": {
          "import_limit": "Maximum safe grid import. 12000 W is a safe limit for a 15 kW single-phase connection.",
          "export_limit": "Maximum permitted grid export. 10000 W is a typical network rule for solar installations.",
          "ramp_step": "Maximum change in battery command per tick. Smaller = smoother; larger = faster response.",
          "plan_stale_minutes": "If the EMHASS plan has not been updated in this many minutes, the coordinator falls back to a zero grid-exchange target.",
          "mpc_sign_inverted": "EMHASS convention: positive mpc_grid_power means export to grid. Leave on unless you have confirmed your EMHASS instance uses the opposite convention."
        }
      }
    },
    "abort": {
      "already_configured": "Grid Coordinator is already configured."
    }
  }
}
